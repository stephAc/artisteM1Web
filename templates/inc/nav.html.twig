<nav class="navbar navbar-expand-lg navbar-light bg-light" style="margin-bottom: 20px;">
    <a class="navbar-brand" href="{{ url('homepage.index') }}">Titos</a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNav">
        <ul class="navbar-nav">
            <li class="nav-item active">
                <a class="nav-link" href="{{ url('homepage.index') }}">Accueil <span class="sr-only">(current)</span></a>
            </li>
            <li class="nav-item dropdown">
                <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                    Oeuvres
                </a>
                <div class="dropdown-menu" aria-labelledby="navbarDropdown">
                    {% for category in categorys %}
                        <a class="dropdown-item" href="{{ url('artworks.index', {"categoryId": category.id}) }}">{{ category.name | capitalize }}</a>
                    {% endfor %}
                </div>
            </li>
            <li class="nav-item">
                <a class="nav-link" href="{{ url('exhibition.index') }}">Expositions</a>
            </li>
            <li class="nav-item">
                <a class="nav-link" href="{{ url('contact.form') }}">Contact</a>
            </li>
            {% if is_granted('ROLE_ADMIN') %}
                <li>
                    <a class="nav-link" href="{{ url('admin.homepage.index') }}">Administration</a>
                </li>
            {% endif %}
        </ul>
    </div>
    {% if is_granted('IS_AUTHENTICATED_FULLY') %}
        <a class="nav-link" href="{{ logout_url('main') }}" style="color: black">Déconnexion</a>
    {% else %}
        <a class="nav-link" href="{{ url('security.login') }}" style="color: black">Connexion</a>
    {% endif %}
</nav>